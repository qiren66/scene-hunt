# 影视取景地打卡 App — MVP 开发规划说明书

> **产品代号**：SceneHunt（场景猎手）
> **版本**：MVP v0.1（第一阶段：核心验证期）
> **规划日期**：2026-04-27
> **目标周期**：4-6 周

---

## 一、项目目标

### 1.1 核心目标

跑通最小价值闭环：

> **搜索一部剧 → 看到名场面 → 在地图上看到取景地 → 打开同款机位对比图 → 拍出电影同款照片**

### 1.2 MVP 成功标准

| 指标 | 标准 |
|---|---|
| 核心闭环完成度 | 用户能完整走完 搜索→详情→地图→对比图→拍照 的全部流程 |
| 首个 IP 数据量 | 至少 1 部作品、50+ 取景地、每个取景地含剧照+实景图 |
| 对比图工具 | 静态对比展示 + 透明叠影相机均可正常使用 |
| 收藏夹 | 本地存储"想去"标记，无需登录 |
| 平台 | 微信小程序可审核上线 + H5 可访问 |

### 1.3 MVP 明确不做

- ❌ 用户注册/登录
- ❌ UGC 投稿功能
- ❌ 社交功能（评论、点赞、关注）
- ❌ 复杂 AR 导航
- ❌ 会员/付费功能
- ❌ 后端服务器

---

## 二、技术架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────┐
│                   UniApp (Vue 3 + TS)           │
│                                                 │
│  ┌───────────┐  ┌───────────┐  ┌──────────────┐ │
│  │   Pages    │  │ Components│  │    Store     │ │
│  │  (4个页面) │  │  (5个组件)│  │ (本地存储)   │ │
│  └─────┬─────┘  └─────┬─────┘  └──────┬───────┘ │
│        │              │               │          │
│  ┌─────┴──────────────┴───────────────┴───────┐  │
│  │              Utils & Data Layer             │  │
│  │  ┌──────────┐ ┌──────────┐ ┌────────────┐  │  │
│  │  │ search.ts│ │  map.ts  │ │works.json  │  │  │
│  │  └──────────┘ └──────────┘ └────────────┘  │  │
│  └─────────────────────────────────────────────┘  │
│                      │                            │
│  ┌───────────────────┴────────────────────────┐   │
│  │            Platform APIs                   │   │
│  │  高德地图 │ 相机 │ 本地存储 │ 图片选择     │   │
│  └────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────┘
         │                    │
    ┌────┴─────┐        ┌────┴─────┐
    │ 微信小程序 │        │  H5 浏览器 │
    └──────────┘        └──────────┘
```

### 2.2 技术选型明细

| 分类 | 选型 | 理由 |
|---|---|---|
| **框架** | UniApp (Vue 3 + TypeScript) | 一套代码 → 小程序 + H5 + App |
| **构建工具** | Vite | UniApp 官方推荐，HMR 快 |
| **状态管理** | Pinia | Vue 3 生态首选，轻量 |
| **地图** | 高德地图（小程序 SDK） | 国内精度高，小程序支持好 |
| **数据存储** | 本地 JSON + localStorage | MVP 阶段无需后端 |
| **相机** | UniApp `<camera>` 组件 | 跨平台兼容，支持实时预览 |
| **图片处理** | Canvas API | 实现叠影合成与保存 |
| **CSS 方案** | UniApp 内置 rpx + SCSS | 适配多端屏幕 |

### 2.3 目标平台优先级

1. **微信小程序**（首选）— 免安装、易传播、相机权限完整
2. **H5**（兼容）— 免登录浏览器访问，降低首次体验门槛
3. **App**（后期）— 需要更高级相机功能时再打包

---

## 三、模块划分

### 3.1 页面模块（4 个核心页面）

```
页面流转关系：

搜索首页 ──(点击作品)──→ 作品详情页 ──(点击场景)──→ 地图页 ──(点击标记)──→ 取景地详情页
   │                        │                                          │
   │                        │                                          │
   └──(直接搜索地名)──→ 地图页                                    叠影相机
```

#### P1：搜索首页 `/pages/index/index`

| 区域 | 内容 |
|---|---|
| 顶部 | App Logo + Slogan："带着电影去旅行" |
| 搜索栏 | 全局搜索框，支持剧名/地名/演员名 |
| 热门推荐 | 横向滚动卡片（如《狂飙》《甄嬛传》《哈利·波特》） |
| 最近浏览 | 本地缓存最近查看的作品（最多 5 个） |

#### P2：作品详情页 `/pages/work-detail/index`

| 区域 | 内容 |
|---|---|
| 头图 | 作品海报 + 基本信息叠加层（片名/年份/导演/主演） |
| 剧情简介 | 可折叠文本 |
| 名场面列表 | 纵向卡片流：缩略图 + 场景描述 + 时间戳 |
| 底部操作栏 | "在地图上查看全部取景地" 按钮 |

#### P3：地图页 `/pages/map/index`

| 区域 | 内容 |
|---|---|
| 全屏地图 | 高德地图，显示当前作品的所有取景地标记点 |
| 标记点弹窗 | 点击标记弹出卡片：剧照缩略图 + 地点名称 + 距离 |
| 底部面板 | 可上拉的抽屉面板，显示取景地列表，支持滑动切换 |
| 定位按钮 | 回到用户当前位置 |
| 筛选按钮 | 按场景类型筛选（室内/室外/夜景等） |

#### P4：取景地详情页 `/pages/location-detail/index`

| 区域 | 内容 |
|---|---|
| 顶部 | 取景地名称 + 地址 + 导航按钮 |
| 对比图区域 | Tab 切换：上下对比 / 左右对比 / 叠影模式 |
| 剧照信息 | 出现集数 + 时间戳 + 剧情描述 |
| 幕后故事 | 选景幕后故事卡片（如有） |
| 收藏按钮 | "想去" / "已打卡" 本地标记 |
| 叠影相机入口 | "打开同款机位相机" 按钮 |

### 3.2 组件模块（5 个核心组件）

| 组件 | 职责 | 复用页面 |
|---|---|---|
| `SearchBar.vue` | 搜索输入 + 模糊匹配下拉建议 | P1 |
| `SceneCard.vue` | 名场面卡片（截图+描述+时间戳） | P2, P3 |
| `CompareViewer.vue` | 对比图展示（上下/左右/叠影三种模式） | P4 |
| `OverlayCamera.vue` | 透明叠影相机（半透明剧照+实时画面） | P4 |
| `LocationMarker.vue` | 地图标记点弹窗信息卡 | P3 |

### 3.3 工具模块

| 模块 | 职责 |
|---|---|
| `search.ts` | 模糊搜索算法（支持拼音首字母匹配） |
| `map.ts` | 地图初始化、标记点管理、距离计算 |
| `favorites.ts` (Store) | 收藏夹 CRUD，基于 localStorage 持久化 |

### 3.4 数据模型

#### 作品数据模型 `Work`

```typescript
interface Work {
  id: string;                    // 唯一标识 "kuangbiao"
  title: string;                 // 作品名 "狂飙"
  titleEn?: string;              // 英文名
  year: number;                  // 上映年份 2023
  director: string;              // 导演 "徐纪周"
  actors: string[];              // 主演 ["张译", "张颂文", "李一桐"]
  poster: string;                // 海报图路径
  genre: string[];               // 类型 ["犯罪", "剧情"]
  description: string;           // 剧情简介
  locationCount: number;         // 取景地数量
  locations: Location[];         // 取景地列表
}
```

#### 取景地数据模型 `Location`

```typescript
interface Location {
  id: string;                    // 唯一标识 "kuangbiao-001"
  workId: string;                // 所属作品 ID
  name: string;                  // 地点名称 "三十三墟街"
  address: string;               // 详细地址 "广东省江门市蓬江区..."
  latitude: number;              // 纬度 22.58
  longitude: number;             // 经度 113.08
  sceneDescription: string;      // 剧情描述 "高启强卖鱼的市场"
  episode?: string;              // 出现集数 "第1-3集"
  timestamp?: string;            // 时间戳 "00:15:30"
  screenshotUrl: string;         // 剧中截图路径
  realPhotoUrl: string;          // 实景照片路径
  behindStory?: string;          // 幕后故事
  tags: string[];                // 标签 ["室外", "市场", "经典"]
}
```

#### 收藏数据模型 `Favorite`

```typescript
interface Favorite {
  locationId: string;
  type: "want_to_go" | "checked_in";
  createdAt: number;             // 时间戳
  note?: string;                 // 个人备注
}
```

---

## 四、实施步骤与时间节点

### 第 1 周：项目初始化 + 首页 + 数据层

| 天 | 任务 | 产出 |
|---|---|---|
| D1 | 创建 UniApp 项目，配置 TypeScript、Pinia、SCSS | 可运行的空项目 |
| D2 | 编写 `works.json` 示例数据（狂飙 50 个取景地） | 完整数据文件 |
| D3 | 实现 `search.ts` 模糊搜索 + `favorites.ts` 状态管理 | 工具模块 |
| D4-5 | 开发搜索首页（P1）：搜索栏 + 热门推荐 + 搜索结果 | 可搜索的首页 |

### 第 2 周：作品详情页 + 地图页

| 天 | 任务 | 产出 |
|---|---|---|
| D6-7 | 开发作品详情页（P2）：头图 + 名场面列表 + 场景卡片 | 作品详情页 |
| D8 | 集成高德地图 SDK，实现基础地图展示 | 地图组件 |
| D9-10 | 开发地图页（P3）：标记点 + 弹窗卡片 + 底部面板 | 地图页 |

### 第 3 周：取景地详情页 + 对比图工具

| 天 | 任务 | 产出 |
|---|---|---|
| D11-12 | 开发取景地详情页（P4）：信息展示 + 收藏功能 | 详情页框架 |
| D13-14 | 开发 `CompareViewer.vue`：上下/左右对比模式 | 对比图查看器 |
| D15 | 开发 `OverlayCamera.vue`：透明叠影相机 | 叠影相机 MVP |

### 第 4 周：打磨 + 联调 + 测试

| 天 | 任务 | 产出 |
|---|---|---|
| D16-17 | 全流程联调：搜索→详情→地图→对比→拍照 | 闭环跑通 |
| D18-19 | UI 打磨：动画过渡、加载状态、空状态、错误处理 | 视觉优化 |
| D20 | 真机测试（小程序 + H5），修复兼容性问题 | 测试报告 |
| D21 | 补充数据（剧照/实景图替换为真实素材） | 可演示版本 |

### 第 5-6 周：缓冲期

- 数据完善：补充更多取景地的剧照和实景照片
- 性能优化：图片懒加载、地图标记聚合
- 小程序审核与发布
- 准备第二阶段开发

---

## 五、设计规范

### 5.1 配色方案（电影胶片质感）

| 用途 | 色值 | 预览 |
|---|---|---|
| 主背景 | `#0D0D0D` | 近纯黑 |
| 卡片背景 | `#1A1A2E` | 深蓝黑 |
| 主强调色 | `#D4AF37` | 金色 |
| 辅助强调色 | `#8B0000` | 暗红 |
| 正文字色 | `#E8E8E8` | 浅灰白 |
| 辅助字色 | `#888888` | 中灰 |
| 分割线 | `#2A2A3E` | 暗蓝灰 |

### 5.2 字体规范

| 类型 | 大小 | 字重 |
|---|---|---|
| 大标题 | 36rpx | Bold |
| 页面标题 | 32rpx | SemiBold |
| 卡片标题 | 28rpx | Medium |
| 正文 | 26rpx | Regular |
| 辅助文字 | 22rpx | Regular |
| 标签 | 20rpx | Medium |

### 5.3 交互规范

- 页面切换：右滑返回，带 300ms 过渡动画
- 卡片点击：轻微缩放反馈（scale: 0.98）
- 加载状态：场记板动画（打板动作循环）
- 打卡成功：全屏"Action！"动效 + 震动反馈
- 空状态：戴导演帽的猫咪插画 + 引导文案

### 5.4 图标体系

使用 UniApp 内置图标 + 自定义 SVG 补充：

| 图标 | 用途 |
|---|---|
| 🎬 | 作品/影视相关 |
| 📍 | 取景地标记 |
| 📷 | 相机/打卡 |
| ❤️ | 收藏/想去 |
| ✅ | 已打卡 |
| 🧭 | 导航 |
| 🔄 | 对比图切换 |

---

## 六、风险与应对

| 风险 | 概率 | 影响 | 应对方案 |
|---|---|---|---|
| 高德地图 SDK 在小程序中功能受限 | 中 | 高 | 提前验证叠影相机在 WebView 中的兼容性，备选腾讯地图 |
| 剧照版权问题 | 高 | 高 | MVP 阶段使用用户拍摄实景图为主，剧照仅作小尺寸引用，标注版权来源 |
| 叠影相机性能不佳 | 中 | 中 | 降低叠影图层分辨率，限制缩放范围，低端机降级为静态对比模式 |
| 小程序审核被拒 | 中 | 低 | 确保无用户生成内容，数据完全静态，提前阅读审核指南 |
| 50 个取景地数据收集耗时 | 高 | 中 | 先完成 20 个核心取景地即可上线，后续增量补充 |

---

## 七、里程碑定义

| 里程碑 | 时间 | 交付物 | 验收标准 |
|---|---|---|---|
| **M1：骨架完成** | 第 1 周末 | 项目框架 + 数据层 + 搜索首页 | 搜索能返回正确结果 |
| **M2：地图可见** | 第 2 周末 | 作品详情页 + 地图页 | 能在地图上看到取景地标记 |
| **M3：对比可用** | 第 3 周末 | 详情页 + 对比图 + 叠影相机 | 能拍摄叠影照片 |
| **M4：闭环跑通** | 第 4 周末 | 全流程联调完成 | 从搜索到拍照全流程无阻断 |
| **M5：发布就绪** | 第 6 周末 | 小程序审核通过 | 可对外分享使用 |

---

## 八、验收检查清单

### 功能验收

- [ ] 搜索框输入剧名，能模糊匹配到作品
- [ ] 点击作品进入详情页，展示名场面列表
- [ ] 点击场景进入地图页，标记点位置正确
- [ ] 点击标记点弹出信息卡片
- [ ] 进入取景地详情，能看到对比图（上下/左右）
- [ ] 叠影相机能打开，剧照可叠加在实时画面上
- [ ] 可调节透明度/缩放/旋转
- [ ] 可拍摄并保存叠影照片
- [ ] 收藏"想去"后，数据在本地持久化
- [ ] 重新打开 App，收藏数据仍在

### 体验验收

- [ ] 页面加载 < 2 秒
- [ ] 地图标记点无延迟渲染
- [ ] 叠影相机帧率 ≥ 24fps
- [ ] 无白屏、无 JS 报错
- [ ] 场记板加载动画正常显示
- [ ] 暗色主题下文字清晰可读

### 兼容性验收

- [ ] 微信小程序（iOS）正常运行
- [ ] 微信小程序（Android）正常运行
- [ ] H5 浏览器（Chrome）正常运行
- [ ] H5 浏览器（Safari）正常运行

---

> **下一步**：确认此规划后，立即开始编码，从项目初始化 + 数据层开始。
